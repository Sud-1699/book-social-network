spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_social_network
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: localhost
    port: 1025
    username: bsnmail
    password: bsnmail123
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
        timeout: 3000
        writetimeout: 5000
        connectiontimeout: 5000

  application:
    security:
      jwt:
        secretKey: Tk5kiAvKMwgLe5ICJiuiKu8Up2JEgiyM
        expiration: 8640000